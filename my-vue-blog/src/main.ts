import { createApp } from 'vue' // Импорт функции для создания Vue приложения
import App from './App.vue'     // Главный компонент приложения

// Создаём экземпляр приложения и монтируем его в элемент с id="app"
createApp(App).mount('#app')
